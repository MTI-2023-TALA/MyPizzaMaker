<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FrontPizza.StartPage"
             Title="StartPage">
    <Grid RowSpacing="25" RowDefinitions="Auto,Auto,*" Padding="{OnPlatform Default='30' }">
        <Image Source="{AppThemeBinding Light=pizza.png}"
               Grid.Row="0"
               SemanticProperties.Description=".NET Podcast application logo"
               WidthRequest="250"
               HeightRequest="310"
               VerticalOptions="Center"
               HorizontalOptions="Center" />
        <Button Grid.Row="1"
                HorizontalOptions="Center"
                Text="Créer une commande"
                Clicked="StartCommand"
                ></Button>
        
    </Grid>
</ContentPage>
